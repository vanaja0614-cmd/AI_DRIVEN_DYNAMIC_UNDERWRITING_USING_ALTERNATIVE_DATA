# Copy to `.env` and adjust. Never commit the real `.env`.
# Values here are the same as the defaults — keep them for reference.

# Port for the Vite dev server.
VITE_PORT=5173

# Backend URL that the Vite proxy forwards /api/* requests to.
VITE_API_PROXY_TARGET=http://localhost:8000
